# Quix Project Descriptor
# This file describes the data pipeline and configuration of resources of a Quix Project.

metadata:
  version: 1.0

# This section describes the Deployments of the data pipeline
deployments:
  - name: Generator
    application: generator
    version: latest
    deploymentType: Service
    resources:
      limits:
        cpu: 100
        memory: 200
    variables:
      - name: output
        inputType: OutputTopic
        description: Kafka topic to publish file-reference messages to (matches the processor's input)
        required: true
        value: blob-files
      - name: FILE_NAMES
        inputType: FreeText
        description: Comma-separated blob object keys to cycle through
        value: samples/readings_1.csv,samples/readings_2.csv,samples/readings_3.csv
      - name: PUBLISH_INTERVAL_SECONDS
        inputType: FreeText
        description: Seconds between published messages
        value: 10
  - name: Blob Processor
    application: processor
    version: latest
    deploymentType: Service
    resources:
      limits:
        cpu: 200
        memory: 500
    variables:
      - name: input
        inputType: InputTopic
        description: Kafka topic to consume file-reference messages from
        required: true
        value: blob-files
      - name: FILE_NAME_FIELD
        inputType: FreeText
        description: Field in each message holding the blob object key
        value: file_name
      - name: BLOB_BASE_PATH
        inputType: FreeText
        description: Root path in Quix blob storage (quixportal injects credentials automatically)
        value: ''
    blobStorage:
      bind: true

# This section describes the Topics of the data pipeline
topics:
  - name: blob-files
